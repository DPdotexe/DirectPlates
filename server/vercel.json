{
    "builds": [
      {
        "src": "**/*",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      { "src": "/api/(.*)", "dest": "server/$1" },
      { "src": "/(.*)", "dest": "server/$1" }
    ]
  }
  